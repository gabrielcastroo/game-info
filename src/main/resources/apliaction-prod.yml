spring:
  datasource:
    url: jdbc:h2:file:~/test
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false
    properties:
      hibernate:
      format_sql: false
  h2:
    console:
      enable: false

logging:
  level:
    br:
      com:
        trabalhopoo: INFO